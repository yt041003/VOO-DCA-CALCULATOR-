import './globals.css'; import type { Metadata } from 'next'; import {getSiteUrl} from '@/lib/seo/siteUrl';
const hasProductionData=Boolean(process.env.MARKET_DATA_API_URL);
export const metadata:Metadata={metadataBase:new URL(getSiteUrl()),title:{default:'VOO DCA Calculator – Historical Dollar Cost Averaging Backtest',template:'%s | VOO DCA Calculator'},description:'Calculate how much regular monthly investments in VOO could have grown using historical prices and dividend reinvestment. Explore returns, contributions, and portfolio growth over time.',alternates:{canonical:'/voo-dca-calculator'},openGraph:{title:'VOO DCA Calculator',description:'Backtest monthly VOO investments with prices and dividends.',url:'/voo-dca-calculator',type:'website'},twitter:{card:'summary_large_image',title:'VOO DCA Calculator',description:'Historical dollar-cost averaging backtest for VOO.'},robots:{index:hasProductionData,follow:true}};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
