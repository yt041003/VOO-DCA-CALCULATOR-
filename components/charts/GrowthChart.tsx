'use client';import {Area,CartesianGrid,Legend,Line,ResponsiveContainer,Tooltip,XAxis,YAxis,ComposedChart} from 'recharts';import type {TimelinePoint} from '@/types/finance';
const money=(v:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(v);
export function GrowthChart({data}:{data:TimelinePoint[]}){const sampled=data.filter((_,i)=>i%Math.max(1,Math.floor(data.length/180))===0||i===data.length-1);return <div className="h-80 w-full" role="img" aria-label="Portfolio value and total contributions over time"><ResponsiveContainer><ComposedChart data={sampled} margin={{top:8,right:8,bottom:4,left:0}}><defs><linearGradient id="value" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#147d58" stopOpacity=".22"/><stop offset="1" stopColor="#147d58" stopOpacity="0"/></linearGradient></defs><CartesianGrid stroke="#e5ece8" vertical={false}/><XAxis dataKey="date" tickFormatter={v=>v.slice(0,4)} minTickGap={40} tick={{fontSize:12}}/><YAxis tickFormatter={v=>v>=1000?`$${Math.round(v/1000)}k`:`$${v}`} width={55} tick={{fontSize:12}}/><Tooltip formatter={(v:number)=>money(v)} labelFormatter={v=>new Date(`${v}T12:00:00`).toLocaleDateString()}/><Legend/><Area type="monotone" dataKey="portfolioValue" name="Portfolio value" stroke="#147d58" strokeWidth={3} fill="url(#value)"/><Line type="monotone" dataKey="contributions" name="Contributions" stroke="#63766d" strokeWidth={2} dot={false} strokeDasharray="5 5"/></ComposedChart></ResponsiveContainer></div>}
